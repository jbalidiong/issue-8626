import { initializeApp } from "firebase/app";

const firebaseConfig = {
    //Firebase Config
  };
                                                                 
const app = initializeApp(firebaseConfig);

export {app};
